This directory contains the list of textures defined using the Make Textures From EOL API page. 

The textures are linked to a database, and should not be deleted unless their corresponding database entries are removed also.
